import { CodeBlockPipe } from './code-block.pipe';

describe('CodeBlockPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeBlockPipe();
    expect(pipe).toBeTruthy();
  });
});
